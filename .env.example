# Optional. When set, RiskPulse turns deterministic findings into a concise
# board report using Claude. The application remains fully functional without it.
ANTHROPIC_API_KEY=

# Optional local database location. Defaults to ./data/riskpulse.db
RISKPULSE_DATABASE_URL=
